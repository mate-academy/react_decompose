import React from 'react';
import './greeting.css';

const Greeting = () => (
  <section className="greeting">
    <span className="greeting__text">Sticky Header!</span>
  </section>
)

export default Greeting;
